审查 fashion-imagegen 项目（C:\Users\Administrator\fashion-imagegen）的母图生成 fallback 流程，确认以下问题是否成立（只诊断，不改代码）。

背景：Codex 配额超限时，母图生成失败，预期应触发 Seedream fallback，但实际没有。任务卡在 waiting_recovery。

嫌疑代码：
- app/queue/worker.py 的 _recover_generation（约203-318行）：在 result.soft_timed_out or result.return_code != 0 时，任务被 transition 到 WAITING_RECOVERY（310-313行），然后调用 self.recovery.recover()（315行）。recovery.recover() 在 app/services/recovery.py 里超时会抛 RecoveryError（99-101行）。
- _process_master 在 539-553行调用 _recover_generation，然后 if recovered is None 时调用 _generate_master_seedream fallback。

需确认：
1. _recover_generation 第315行调用 recovery.recover() 时，RecoveryError 是否被捕获？是否会导致异常向上传播，绕过第544行的 fallback？
2. 第539行 _recover_generation 调用处是否有 try/except 包裹？
3. Codex 配额超限（rc=1, image_generated=False）时，实际走的是哪个分支？确认任务会卡在 waiting_recovery 还是走 fallback。

请阅读相关代码，确认根因，输出：问题确认/不确认 + 具体代码位置和证据。

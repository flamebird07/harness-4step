为 fashion-imagegen 项目（C:\Users\Administrator\fashion-imagegen）设计修复方案，解决 Codex 失败触发 Seedream fallback 时任务被卡住/重试计数被错误消耗的问题。只出方案，不改代码。

已确认的问题（Step 1 审查，app/queue/worker.py）：
【问题1 高】双重消耗重试次数：Codex 失败路径 _retry_or_finalize 已被调用一次（入队+retry_count+1），若 Seedream 也失败，_generate_master_seedream 的 except 分支再次调用 _retry_or_finalize，再加一次。auto_retry_limit=3 时同时失败的话有效尝试减半。
【问题2 高】review 失败分支（约640行）用过期 task.retry_count（方法开头读取），但 Codex 失败已通过 retry() 把 DB retry_count 加1。反复叠加后超过3次上限，retry() 抛 ValueError（task_service.py:274-278）未被捕获，冒泡到 _run 才被 _fail_safely 兜住，任务卡住。
【问题3 中】review 失败后 retry() 但没调 enqueue（约646-647行），任务卡在 QUEUED。纯 Codex 路径是既有 bug；fallback 路径下被 _retry_or_finalize 残留的队列条目恰好掩盖。
【问题4 中】fallback 会复活已到重试上限的终态任务，且 DB retry_count 已加1但重试从未真正执行。

修复方向（claude 建议）：
1. 把 Seedream 兜底从重试账本解耦：Codex 失败触发 fallback 时，except 块不调 _fail_safely（它会 retry 入队+加计数），改为只把任务置为终态 FAILED（不 retry），让 _generate_master_seedream 内部接管状态转换（FAILED→QUEUED→RUNNING→Seedream生成）。Seedream 失败后再统一走一次 _retry_or_finalize。
2. review 失败分支改用重新 get() 的最新 retry_count，并补上 enqueue。

请设计完整修复方案，具体到改哪个文件的哪几行、改成什么。重点覆盖：
- except 块（约544-551行）如何处理才不触发 retry/清空目录？
- _generate_master_seedream（约649-669行）如何正确接管状态？
- review 失败分支（约640-647行）如何用最新 retry_count + enqueue？
- 是否保持其他路径（Codex 正常、子图编辑）不变？

只出方案不改代码。

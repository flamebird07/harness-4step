审查 fashion-imagegen 项目（C:\Users\Administrator\fashion-imagegen）app/queue/worker.py 的 _process_master 中，Codex 失败触发 Seedream fallback 时的处理逻辑，确认以下问题（只诊断，不改代码）。

背景：Codex 配额超限时，_process_master 的 except 块捕获 RuntimeError/RecoveryError，调用 _fail_safely(task_id, ...)，然后设 recovered=None 触发 Seedream fallback。实测发现：Seedream 成功生成母图后，任务又报 "No such file or directory: '...results\xxx.jpg'" 并重试，retry_count 被消耗。

嫌疑代码：
- _fail_safely（约173-177行）：调用 _retry_or_finalize(task_id, FAILED, message)
- _retry_or_finalize（约136-163行）：若 retry_count < auto_retry_limit，会 transition(FAILED) → retry()（清空 output_dir/results 目录、retry_count+1、重新入队）→ 返回 True
- _process_master except 块（约544-551行）：调用 _fail_safely 后设 recovered=None
- _generate_master_seedream（约649-669行）：内部检查 FAILED/TIMEOUT 转 QUEUED，再 RUNNING，调用 seedream.generate() 生成到 seedream-raw 目录

需确认：
1. except 块调用 _fail_safely 后，_retry_or_finalize 是否因为 retry_count < limit 而执行 retry()（清空目录+重新入队+消耗retry计数）？
2. 若 _fail_safely 已 retry（清空 output_dir/results），Seedream 生成后又继续 _process_master 的后续处理（copy2 到 results）时，是否因目录被清空而报 FileNotFoundError？
3. 这个交互是否导致 Seedream 母图被清空、任务卡住、retry 计数被消耗？
4. 正确的修复方向：except 块是否应该只置终态（不 retry），让 _generate_master_seedream 内部接管状态转换？

请阅读相关代码，确认问题，输出：问题确认/不确认 + 代码位置和证据 + 推荐的修复方向。
